import * as React from "react";

interface HomeProps {}

const Home = (props: HomeProps) => {
    return (
        <div>
            <h1>Welcome to the bookstore!</h1>
        </div>
    );
};

export default Home;
